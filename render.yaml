services:
  # Frontend Service (Static Site - FREE)
  - type: web
    name: course-registration-frontend
    env: static
    plan: starter
    buildCommand: npm install && npm run build
    staticPublishPath: build
    envVars:
      - key: REACT_APP_API_URL
        value: https://your-backend-url.railway.app/api 